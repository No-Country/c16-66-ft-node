
export const CalendarEvent = ({event}) => { // props mirar como enviar un turno por props, o si se requiere

   const { title }= event;

      
    return (
     <>
  
     <strong></strong>
     <span>{title}</span>
     </>
    )
  }
  
  